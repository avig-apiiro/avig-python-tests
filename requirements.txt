# Runtime + codegen dependencies for the U2 gRPC services.
# Install into a project venv:
#   python -m venv .venv
#   .venv/bin/pip install -r requirements.txt
grpcio==1.82.1
grpcio-tools==1.82.1
protobuf==7.35.1
googleapis-common-protos==1.75.0
